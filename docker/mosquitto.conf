# ── Mosquitto MQTT Broker 配置 ─────────────────────────────
# 本地开发环境允许匿名访问，生产环境请启用密码认证

listener 1883
allow_anonymous true

# 持久化（可选）
persistence true
persistence_location /mosquitto/data

# 日志
log_dest stdout
log_type all
